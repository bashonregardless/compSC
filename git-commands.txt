Thu Jul  3 12:37:26 IST 2019
--> Release a change
  git checkout 1x
  git pull --rebase origin 1x
  git checkout master
  git pull --rebase origin master
  git checkout 1x
  git rebase master
  git push origin 1x

--> build commit
  npm run buid
  git add public -f
  git commit "build: build"
  git push origin 1x

--> Release
  git checkout 1x
  git pull --release origin 1x
  git checkout master
  git pull --rebase origin master
  git checkout 1x
  git rebase master
  git checkout master
  git merge 1x
  git push master
  npm run release <| major | minor | patch | premajor | preminor | prepatch | prerelease> 

--> Pre Deployment steps
  A commit before 'npm run build'
  Push upstream
  After 'npm run build', commit public folder changes
  Push upstream

  Go to Engine Server root directory : on branch staging
	git add public/ -f  ( -f is required since these files are gitignored )
	commit some change
	commit message: 'chore(build): bundled files'
	git push

** Always rebase before merging both feat-survey and 1x
  git pull --rebase origin feat-survey
  git pull --rebase origin 1x
  git merge feat-survey
  git add public -f
  git commit -m "build: build"
  git push origin 1x
  /* This triggers development build */
