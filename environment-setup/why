---> Why when I change `source path/to/bash_profile` to `bash path/to/bash_profile` in bash_profile.template, does tmux not open on sourcing ~/.bsah_profile?

---> Why does not the command `export $VIMCOFIG = path/to/vimconfig` add variable to PATH when I change `source path/to/bash_profile` to `bash path/to/bash_profile` in bash_profile.template?
